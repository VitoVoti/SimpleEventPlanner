import Link from "next/link";
import { styled } from '@mui/system';

// styled with MUI utilities
const CustomLink = styled(Link)({
    color: "white",
    textDecoration: "none",
    "&:hover": {
        textDecoration: "underline",
    },
    "&:visited": {
        color: "white",
    },

});

export default CustomLink;