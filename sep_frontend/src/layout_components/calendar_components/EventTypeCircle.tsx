import { Circle } from "@mui/icons-material";

function EventTypeCircle({ color }: { color: string }) {
    return <Circle sx={{ color: color, fontSize: '0.65em', marginRight: 1}} />
}

export default EventTypeCircle